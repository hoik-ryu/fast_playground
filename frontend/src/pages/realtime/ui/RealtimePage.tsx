// 나중에 WebSocket / MQTT 테스트 화면을 붙일 자리(placeholder).
// 예: WebSocket 연결 버튼, 수신 메시지 로그, 발행(publish) 입력창 등.
export function RealtimePage() {
  return (
    <div>
      <h2>Realtime (WebSocket / MQTT)</h2>
      <p style={{ color: '#888' }}>
        여기에 WebSocket / MQTT 연결 및 메시지 송수신 테스트 UI를 추가할 예정입니다.
      </p>
      <ul style={{ color: '#888' }}>
        <li>WebSocket 연결/해제 버튼</li>
        <li>실시간 수신 메시지 로그</li>
        <li>메시지 발행(publish) 입력창</li>
      </ul>
    </div>
  );
}
